# Kiểm tra production ngày 27/08/2026

## Bằng chứng đã kiểm tra

- Kết nối bằng user token được chủ tài khoản cấp, trong phiên trình duyệt kiểm thử riêng. Không đăng bài hoặc gửi tin khách để thử.
- Token thuộc app Postapp, App ID `990458834013160`; có quyền Pages messaging, metadata và posts.
- 10 Page ngoài Minh Mạng, Phú Mỹ An, Đất Xanh Huế, Royal Park trả HTTP 200 khi đọc hội thoại; đều có app Postapp đăng ký `messages` và `standby`.
- Phiên mới tải 377 hội thoại. Page `110129951561478` tải 50 hội thoại; Meta trả 1.057 lượt thích tại thời điểm kiểm tra.
- Ô nhập chat nằm trong viewport PC 1249x1221 và mobile giả lập 390x844; đã kiểm tra mở chat và quay lại danh sách.
- Giao diện soạn bài mở được; không có lỗi/warning console trong lượt kiểm tra cuối.
- Trace Inbox đã đăng nhập sau sửa: LCP 1.298 giây, CLS 0.00, không giới hạn CPU/mạng. Đây là đo phòng thử nghiệm, không phải dữ liệu toàn bộ người dùng hay phép so sánh A/B cùng hội thoại.
- `npm run check`: 82/82 test và production build đạt; `api/push/status.js` và service worker được kiểm tra cú pháp riêng.

## Các lỗi đã sửa

- Nhận tin webhook trong cả `messaging` và `standby`; bỏ echo, dedupe theo message ID.
- Đọc đăng ký webhook trước khi sửa; giữ nguyên các trường đang dùng và không ghi lại khi đã có đủ `messages`, `standby`.
- Xác minh token thuộc app của backend; chỉ đăng ký các Page giao diện đã chọn, giao với danh sách Meta cho phép.
- Không dùng một webhook của Page A để tắt âm báo dự phòng của tất cả Page khác; chỉ dựa vào Push thật từng Page trên thiết bị còn bật quyền.
- Phân biệt gửi yêu cầu test, nhận Push thật, lỗi App Secret và callback chưa xác minh. Không báo thành công màn hình khóa chỉ vì dịch vụ Push nhận yêu cầu.
- Chừa kích thước ảnh chat để hạn chế giật bố cục.
- Giữ cache của Page tải lỗi khi những Page khác tải thành công; không xóa lựa chọn Page khi sync nền lỗi.
- Không báo đăng bài thành công nếu Meta chưa trả ID. Xác nhận thủ công trước khi thử lại, nhất là khi mất mạng khiến kết quả chưa rõ.
- Thêm `POST /api/push/status`, yêu cầu token, chỉ đọc chẩn đoán của các Page tài khoản quản lý. Không trả khóa, nội dung chat, ID tin nhắn hoặc endpoint thiết bị.

## Điểm còn chặn thông báo nền

Chẩn đoán production trả HTTP 200, nhưng `appCredentialsValid=false`:

- App token dạng ghép bị Meta từ chối với #190 `Invalid OAuth access token signature.`
- Xác thực chính thức `oauth/access_token` bằng client credentials bị từ chối với #1 `Error validating client secret.`
- Mỗi Page trong 10 Page có 1 đăng ký thiết bị còn hiệu lực. Không có sự kiện webhook trong bảng theo dõi hiện tại (dữ liệu sự kiện được dọn sau 7 ngày).
- Chưa đọc được callback do App Secret bị từ chối; không kết luận callback bị thiếu hay sai URL khi chưa đọc được.

Chủ tài khoản cần đối chiếu App Secret hiện tại của app `990458834013160` và cập nhật `META_APP_SECRET` ở môi trường Production của Vercel. Không gửi App Secret trong chat. Sau đó redeploy, gọi lại chẩn đoán; kiểm tra callback `https://metapost-studio.vercel.app/api/webhook`, thử tin khách thật trên thiết bị khóa màn hình. Chưa xác nhận âm thanh/hiển thị trên iPhone vật lý.

Nguyên nhân lần đăng 5 Page chỉ thành công 4 Page trước đây chưa xác định vì chưa có phản hồi lỗi của lần đó. Không có bài đăng thử nào được tạo trong lượt audit này.

## Bản triển khai

- Production: https://metapost-studio.vercel.app/
- Deployment: `dpl_3MFEPy7jvFp3oEYWid7LYEdgr6QN`
- Bundle chính: `index-DqC_NFye.js`; service worker v6.

## Kiểm tra lại ngày 28/08/2026

- Vercel xác nhận `META_APP_SECRET` (sensitive, Production) đã được cập nhật lúc 17:59:47 ngày 27/08/2026, giờ Việt Nam. Không đọc hoặc xuất giá trị khóa.
- Đã redeploy đúng bản release trước để nạp biến môi trường mới: `dpl_Az6N8gp5SeQeEp9FFpsowBx9k5fk`, alias production xác nhận Ready lúc 03:46 ngày 28/08.
- Gọi lại endpoint chẩn đoán đã xác thực sau redeploy: `appCredentialsValid=false`, Meta #1 `Error validating client secret.`. Vẫn chưa đọc được callback và chưa có sự kiện webhook được ghi nhận.
- Token người dùng vẫn thuộc app Postapp `990458834013160` (Meta HTTP 200). Cần chủ tài khoản đối chiếu lại đúng App Secret của app này; không dùng Access Token, Client Token hay mã xác minh webhook thay thế.
- Không thay đổi đăng ký Page, gửi tin khách hoặc đăng bài trong lượt kiểm tra lại này.

### Kết quả sau lần cập nhật khóa tiếp theo, 03:52 ngày 28/08

- Đã redeploy release hiện có với cấu hình mới: `dpl_BEnGoxoc5RzXzMMP6PYh1bjtmhhL`; alias production Ready.
- Meta xác nhận `appCredentialsValid=true`, `callbackCheckAvailable=true`, `callbackReady=true`, không còn lỗi xác thực App Secret.
- Callback đúng `https://metapost-studio.vercel.app/api/webhook`; có `messages`, `standby`, read/echo/delivery/handover/postback.
- Đọc trực tiếp đăng ký Meta của cả 10 Page trong phạm vi: HTTP 200, đều có `messages` và `standby`; không sửa đăng ký hay bật lại 4 Page đã loại trừ.
- Chín Page có một đăng ký thiết bị còn hiệu lực. Page Mua Bán Nhà Đất Huế (`523909157982664`) không có thiết bị nhận Push; cần chọn Page trên thiết bị muốn nhận rồi bấm bật/kiểm tra chuông.
- Vẫn chưa có sự kiện tin nhắn mới trong bảng theo dõi (`lastWebhookAt=null`). Cấu hình đã được xác minh, nhưng chưa được coi là bằng chứng điện thoại nhận/đổ chuông khi khóa màn hình. Cần tin thử từ tài khoản cá nhân khác gửi vào Page và đối chiếu log sau đó.
