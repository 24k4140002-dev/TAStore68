# TAStore68 Pro — Environment Variables Template
# Copy this file to .env.local for local development, or add these variables in Vercel Project Settings.

# Meta Developer App Credentials (Required for permanent token exchange)
META_APP_ID=your_facebook_app_id_here
META_APP_SECRET=your_facebook_app_secret_here

# Meta Graph API Version (Server and browser default: v26.0)
META_GRAPH_VERSION=v26.0
VITE_META_GRAPH_VERSION=v26.0

# Meta webhook verification token (Required only when enabling /api/webhook)
FB_WEBHOOK_VERIFY_TOKEN=generate_a_long_random_value_here

# Private storage used by Web Push. Never expose the service-role key to Vite.
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your_sb_secret_or_legacy_service_role_key_here

# Web Push VAPID keys (`npx web-push generate-vapid-keys --json`)
WEB_PUSH_VAPID_PUBLIC_KEY=your_public_vapid_key_here
WEB_PUSH_VAPID_PRIVATE_KEY=your_private_vapid_key_here
WEB_PUSH_SUBJECT=mailto:admin@example.com
